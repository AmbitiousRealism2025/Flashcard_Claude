export const APP_CONFIG = {
  NAME: 'AI Flashcards',
  VERSION: '1.0.0',
  DESCRIPTION: 'Learn AI concepts through interactive flashcards',
};

export const STORAGE_KEYS = {
  THEME: '@ai_flashcards_theme',
  PROGRESS: '@ai_flashcards_progress',
  SETTINGS: '@ai_flashcards_settings',
};

export const GESTURE_CONFIG = {
  SWIPE_THRESHOLD: 50,
  SWIPE_VELOCITY_THRESHOLD: 0.3,
  TAP_THRESHOLD: 200,
};
